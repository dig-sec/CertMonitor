version: "3.8"

services:
  certmonitor:
    build:
      context: .
    container_name: certmonitor
    env_file:
      - .env
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
